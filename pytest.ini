[pytest]
# Registered so `-m "not integration"` works and the marker stops raising
# PytestUnknownMarkWarning. There was no pytest config in this repo at all.
markers =
    integration: needs a real PostgreSQL database; skipped automatically when none is configured
    unit: pure unit test, no I/O

testpaths = tests
